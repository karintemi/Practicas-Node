<!DOCTYPE html>
html(lang="en")
  head
    include partials/header
  body.login
    
    .contenedor-formulario
      
      h1 Iniciar Sesión en UpTask
      
      if locals.mensajes
        .alertas
          each categoria in Object.keys(locals.mensajes)
            each error in mensajes[categoria]
              .alerta(class=`${categoria}`)
                p= error
      
      form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
        .campo
          label(for="email") E-mail:
          input(type="email" name="email" placeholder="Tu E-mail" value=email)
        .campo
          label(for="password") E-mail:
          input(type="password" name="password" placeholder="Tu password/contraseña" value=password)            
        .campo.enviar
          input(type="submit" value="Iniciar Sesión")
        .campo.acciones
          a(href="/crear-cuenta") Si no tienes Cuenta, crea una GRATIS!
          a(href="/reestablecer") Olvidaste tu password/contraseña?
          
