<!DOCTYPE html>
html(lang="en")
  head
    include partials/header
  body.login
    .contenedor-formulario
      
      h1= nombrePagina
      p Ingresa tu nueva contraseña/password
      
      form.caja-login(method="POST")
        .campo
          label(for="password") Password/Contraseña:
          input(type="password" name="password" placeholder="Nueva password/contraseña")
        .campo.enviar
          input.boton(type="submit" value="Reestablecer Password/Contraseña")
