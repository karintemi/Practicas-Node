<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X_UA_Compatible", content="ie=edge")
    style 
      include ./email.css
      
  body
    h2 Confirma tu cuenta UpTask
    p Ya estas casi inscripto, solo presiona el siguiente botón
    a (href=`${confirmarUrl}`) Resetar Password/Contraseña
    p Si no puedes acceder al enlace, visita #{confirmarUrl}
    p Si no solicitaste este e-mail puedes ignorarlo
