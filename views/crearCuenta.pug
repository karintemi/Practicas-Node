<!DOCTYPE html>
html(lang="en")
  head
    include partials/header
  body.login
    
    .contenedor-formulario
      
      h1 Crea tu cuenta en UpTask
        
      if locals.mensajes
        .alertas
          each categoria in Object.keys(locals.mensajes)
            each error in mensajes[categoria]
              .alerta(class=`${categoria}`)
                p= error
      form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
        .campo
          label(for="email") E-mail:
          input(type="email" name="email" placeholder="Tu E-mail" value=email required)
        .campo
          label(for="password") E-mail:
          input(type="password" name="password" placeholder="Tu password/contraseña" value=password required)            
        .campo.enviar
          input(type="submit" value="Crear cuenta")
        .campo
          a(href="/iniciar-sesion") Volver a Iniciar Sesión
          
